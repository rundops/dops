name: git-summary
version: 1.0.0
description: Summarize recent commits for a git repo
risk_level: low
script: script.sh
parameters:
  - name: path
    type: string
    required: true
    description: Path to the git repository
    scope: runbook
    default: .
